[project]
name = "agents-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["openai", "pydantic", "typing", "dotenv", "ipykernel", "jupyter"]
